{
    "scripts": {
        "deploy": "aws cloudformation deploy --stack-name=SqsTest --template-file=template.yml --capabilities=CAPABILITY_NAMED_IAM",
        "remove": "aws cloudformation delete-stack --stack-name=SqsTest"
    }
}
